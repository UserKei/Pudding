# Third-Party Assets

Put downloaded external asset packs here, grouped by source pack.

Recommended layout:

```text
assets/third_party/<asset_pack_name>/
  original/
  LICENSE.txt
  SOURCE.txt
```

Keep original downloads or extracted files here as reference. Copy only the files actually used by the game into `assets/game/`, and make scenes reference the `assets/game/` copies.
